<UserControl x:Class="Resumetry.Views.SankeyReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Resumetry.ViewModels"
             xmlns:controls="clr-namespace:Resumetry.WPF.Controls"
             xmlns:converters="clr-namespace:Resumetry.Converters"
             d:DataContext="{d:DesignInstance Type=vm:SankeyReportViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1200"
             Loaded="SankeyReportView_OnLoaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Application Status Flow" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Style="{StaticResource SubHeaderTextStyle}">
                    <Run Text="Sankey diagram of status transitions across"/>
                    <Run Text="{Binding TotalApplications, Mode=OneWay}"/>
                    <Run Text="applications"/>
                </TextBlock>
            </StackPanel>

            <!-- Back Button -->
            <Button Grid.Column="1"
                    Content="← Back to Applications"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding GoBackCommand}"
                    Height="36"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Sankey Diagram Container -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="20">
            <Grid>
                <!-- Sankey Diagram Control -->
                <controls:SankeyDiagramControl ReportData="{Binding ReportData}"
                                                Visibility="{Binding IsLoaded, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
