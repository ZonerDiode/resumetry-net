<Window x:Class="Resumetry.Views.ApplicationFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Resumetry.Views"
        xmlns:vm="clr-namespace:Resumetry.ViewModels"
        xmlns:converters="clr-namespace:Resumetry.Converters"
        xmlns:helpers="clr-namespace:Resumetry.Helpers" 
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="1200" Width="1100"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <converters:IndexPlusOneConverter x:Key="IndexPlusOneConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBlueBrush}" Padding="24,16">
            <TextBlock Text="Resumetry" FontSize="24" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Background="{StaticResource LightGrayBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="32,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title and Action Buttons -->
                    <Grid Grid.Row="0" Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding WindowTitle}" Style="{StaticResource HeaderTextStyle}"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="{Binding SaveButtonText}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="CreateButton_Click"
                                    Height="36"
                                    Margin="0,0,8,0"/>
                            <Button Content="Cancel"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="CancelButton_Click"
                                    Height="36"/>
                        </StackPanel>
                    </Grid>

                    <!-- Basic Information Section -->
                    <Border Grid.Row="1" Background="{StaticResource LightBlueBrush}"
                            Padding="24" Margin="0,0,0,16" CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Section Header -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                                       Text="Basic Information"
                                       Style="{StaticResource SectionHeaderStyle}"
                                       Margin="0,0,0,16"/>

                            <!-- Company -->
                            <StackPanel Grid.Row="1" Grid.Column="0">
                                <TextBlock Text="Company *" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding Company, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         Height="36"/>
                            </StackPanel>

                            <!-- Role -->
                            <StackPanel Grid.Row="1" Grid.Column="2">
                                <TextBlock Text="Role *" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding Role, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         Height="36"/>
                            </StackPanel>

                            <!-- Recruiter Name -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,16,0,0">
                                <TextBlock Text="Recruiter Name" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding RecruiterName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         helpers:WatermarkHelper.Watermark="e.g., Jane Smith"
                                         Height="36"/>
                            </StackPanel>

                            <!-- Recruiter Company -->
                            <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,16,0,0">
                                <TextBlock Text="Recruiter Company" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding RecruiterCompany, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         helpers:WatermarkHelper.Watermark="e.g., Acme Recruiting"
                                         Height="36"/>
                            </StackPanel>

                            <!-- Bottom Row: Applied Date, Salary, Top Job -->
                            <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Applied Date -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Applied Date *" Style="{StaticResource FormLabelStyle}"/>
                                    <DatePicker SelectedDate="{Binding CreatedAt}"
                                                Style="{StaticResource ModernDatePickerStyle}"
                                                Height="36"/>
                                </StackPanel>

                                <!-- Salary -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Salary" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding Salary, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource ModernTextBoxStyle}"
                                             helpers:WatermarkHelper.Watermark="e.g., $80,000 - $100,000"
                                             Height="36"/>
                                </StackPanel>

                                <!-- Mark as Top Job -->
                                <CheckBox Grid.Column="4"
                                          IsChecked="{Binding MarkAsTopJob}"
                                          Content="Mark as Top Job"
                                          VerticalAlignment="Bottom"
                                          Margin="0,0,0,8"
                                          FontSize="14"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Status and Notes Side by Side -->
                    <Grid Grid.Row="2" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="320"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Status Section -->
                        <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Header with Add Button -->
                                <Grid Grid.Row="0" Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Style="{StaticResource SectionHeaderStyle}">
                                        <Run Text="Status ("/>
                                        <Run Text="{Binding StatusCount, Mode=OneWay}"/>
                                        <Run Text=")"/>
                                    </TextBlock>

                                    <Button Grid.Column="1"
                                            Content="+ Add"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Command="{Binding AddStatusCommand}"
                                            Height="32"
                                            Padding="12,4"/>
                                </Grid>

                                <!-- Status Items -->
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding StatusItems}" AlternationCount="100">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource LightGrayBrush}"
                                                    CornerRadius="4"
                                                    Padding="8"
                                                    Margin="0,0,0,12">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Header -->
                                                    <Grid Grid.Row="0" Margin="0,0,0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- Use AlternationIndex (zero-based). Use converter to display 1-based -->
                                                        <TextBlock Grid.Column="0"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="14">
                                                            <Run Text="Status "/>
                                                            <Run Text="{Binding Path=(ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWay, Converter={StaticResource IndexPlusOneConverter}}"/>
                                                        </TextBlock>

                                                        <Button Grid.Column="1"
                                                                Content="Remove"
                                                                Style="{StaticResource DangerButtonStyle}"
                                                                Command="{Binding DataContext.RemoveStatusCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"
                                                                Height="28"
                                                                Padding="12,4"/>
                                                    </Grid>

                                                    <!-- Fields -->
                                                    <Grid Grid.Row="1">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="16"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="Date" Style="{StaticResource FormLabelStyle}"/>
                                                            <DatePicker SelectedDate="{Binding Occurred}"
                                                                        Style="{StaticResource ModernDatePickerStyle}"
                                                                        Height="36"/>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="2">
                                                            <TextBlock Text="Status" Style="{StaticResource FormLabelStyle}"/>
                                                            <ComboBox SelectedItem="{Binding Status}"
                                                                      ItemsSource="{Binding AvailableStatuses}"
                                                                      Style="{StaticResource ModernComboBoxStyle}"
                                                                      Height="36"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>

                        <!-- Notes Section -->
                        <Border Grid.Column="2" Background="White" CornerRadius="4" Padding="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Header with Add Button -->
                                <Grid Grid.Row="0" Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Style="{StaticResource SectionHeaderStyle}">
                                        <Run Text="Notes ("/>
                                        <Run Text="{Binding NotesCount, Mode=OneWay}"/>
                                        <Run Text=")"/>
                                    </TextBlock>

                                    <Button Grid.Column="1"
                                            Content="+ Add"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Command="{Binding AddNoteCommand}"
                                            Height="32"
                                            Padding="12,4"/>
                                </Grid>

                                <!-- Notes Items -->
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding ApplicationEvents}" AlternationCount="100">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource LightGrayBrush}"
                                                    CornerRadius="4"
                                                    Padding="8"
                                                    Margin="0,0,0,12">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Header -->
                                                    <Grid Grid.Row="0" Margin="0,0,0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="14">
                                                            <Run Text="Note "/>
                                                            <Run Text="{Binding Path=(ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWay, Converter={StaticResource IndexPlusOneConverter}}"/>
                                                        </TextBlock>

                                                        <Button Grid.Column="1"
                                                                Content="Remove"
                                                                Style="{StaticResource DangerButtonStyle}"
                                                                Command="{Binding DataContext.RemoveNoteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"
                                                                Height="28"
                                                                Padding="12,4"/>
                                                    </Grid>

                                                    <!-- Date Field -->
                                                    <StackPanel Grid.Row="1" Margin="0,0,0,12">
                                                        <TextBlock Text="Date" Style="{StaticResource FormLabelStyle}"/>
                                                        <DatePicker SelectedDate="{Binding Occurred}"
                                                                    Style="{StaticResource ModernDatePickerStyle}"
                                                                    Height="36"/>
                                                    </StackPanel>

                                                    <!-- Description Field -->
                                                    <StackPanel Grid.Row="2">
                                                        <TextBlock Text="Description" Style="{StaticResource FormLabelStyle}"/>
                                                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                                                 TextWrapping="Wrap"
                                                                 AcceptsReturn="True"
                                                                 Height="80"
                                                                 VerticalScrollBarVisibility="Auto"
                                                                 Style="{StaticResource ModernTextBoxStyle}"
                                                                 helpers:WatermarkHelper.Watermark="What happened on this date..."/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Additional Information Section -->
                    <Border Grid.Row="3" Background="{StaticResource LightBlueBrush}"
                            Padding="24" Margin="0,0,0,16" CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Section Header -->
                            <TextBlock Grid.Row="0"
                                       Text="Additional Information"
                                       Style="{StaticResource SectionHeaderStyle}"
                                       Margin="0,0,0,16"/>

                            <!-- Source Page URL -->
                            <StackPanel Grid.Row="1" Margin="0,0,0,16">
                                <TextBlock Text="Source Page URL" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding SourcePageUrl, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         helpers:WatermarkHelper.Watermark="https://..."
                                         Height="36"/>
                            </StackPanel>

                            <!-- Review Page URL -->
                            <StackPanel Grid.Row="2" Margin="0,0,0,16">
                                <TextBlock Text="Review Page URL" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding ReviewPageUrl, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         helpers:WatermarkHelper.Watermark="https://..."
                                         Height="36"/>
                            </StackPanel>

                            <!-- Login Hints -->
                            <StackPanel Grid.Row="3" Margin="0,0,0,16">
                                <TextBlock Text="Login Hints" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding LoginHints, UpdateSourceTrigger=PropertyChanged}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         Height="40"
                                         VerticalScrollBarVisibility="Auto"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         helpers:WatermarkHelper.Watermark="Notes about accessing the company portal..."/>
                            </StackPanel>

                            <!-- Description -->
                            <StackPanel Grid.Row="4">
                                <TextBlock Text="Description" Style="{StaticResource FormLabelStyle}"/>
                                <Border Height="250" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                    <Grid xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
                                        <wv2:WebView2 x:Name="DescriptionWebView"
                                                      CoreWebView2InitializationCompleted="DescriptionWebView_CoreWebView2InitializationCompleted"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
